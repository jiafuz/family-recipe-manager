# Repository Guidelines

## Project Structure & Module Organization

This repository is a dependency-free, single-page prototype for a family recipe manager.

- `index.html` contains all HTML, CSS, sample data, and JavaScript behavior.
- `.git/` stores repository history and configuration; do not edit it directly.
- `.agents/` is reserved for local agent metadata and currently contains no project source.

Keep new product behavior in `index.html` while the prototype remains small. If the file is split later, use `src/` for JavaScript and styles, `assets/` for images, and `tests/` for automated checks.

## Build, Test, and Development Commands

No build step or package installation is required.

```powershell
python -m http.server 8000
```

Serves the repository locally at `http://localhost:8000`; prefer this over opening the file directly when testing browser behavior.

```powershell
npx.cmd --yes prettier@3.6.2 --check index.html
npx.cmd --yes prettier@3.6.2 --write index.html
git diff --check
```

Use these commands to check formatting, apply formatting, and detect whitespace errors before committing.

## Coding Style & Naming Conventions

- Use two-space indentation for HTML, CSS, and JavaScript.
- Format `index.html` with Prettier after structural edits.
- Use semantic, kebab-case CSS classes such as `.manage-item` and `.today-card`.
- Use camelCase for JavaScript functions and variables, for example `showScreen()` and `cartCount`.
- Name screen IDs by purpose (`home`, `manage`, `recipeEditor`), and keep IDs unique.
- Reuse the CSS variables in `:root` instead of introducing scattered color values.
- Keep user-facing language consistent: use “点菜”, “保存菜单”, “采购清单”, and “保存菜谱” rather than commercial order terminology.

## Testing Guidelines

There is no automated test suite yet. Manually verify all primary flows on desktop and a narrow mobile viewport: navigation, recipe search/filtering, creating and editing recipes, submitting a menu, and updating cooking progress. Check the browser console for JavaScript errors. Add automated tests under `tests/` if logic is extracted from the page.

## Commit & Pull Request Guidelines

Use short Conventional Commit messages, following the existing pattern: `feat: optimize family recipe planning workflow`. Prefer `feat:`, `fix:`, `refactor:`, `style:`, or `docs:`.

Pull requests should include a concise summary, manual test steps, and before/after screenshots for visible UI changes. Keep unrelated changes separate and mention any intentionally unfinished prototype interactions.

## “点菜”功能核心产品需求

后续设计、开发和调整“点菜”功能时，必须遵循以下产品约束：

### 核心目标

- “点菜”服务于家庭内部协作，不照搬商业外卖流程。
- 家庭成员可以提前为今天、明天或指定日期的早餐、午餐、晚餐点菜。
- 所有成员使用同一套点菜、菜单编辑和采购流程；不再设置掌勺人接单或确认菜单环节。
- 菜单保存后立即生效，并实时派生相应日期的采购清单。
- 不询问“几个人吃”，这是家庭场景中的伪需求。

### 家庭成员点菜流程

- 遵循“先选菜，再选择点餐时间，最后提交订单”的使用顺序。
- 从“点菜”一级页面发起普通点餐时，必须完整经历“选菜 → 选择点餐时间 → 提交订单”，不得复用上一次编辑菜单留下的日期或餐次。
- 从“点餐记录”选中某个日期的某一餐进入编辑时，该日期和餐次已经确定；点菜页应明确提示当前正在编辑的餐次，选好菜后直接提交订单，不再重复选择时间。
- 用户在餐次编辑过程中点击底部“点菜”进入一级页面，视为退出本次餐次编辑并开始普通点餐；必须清空餐次锁定、编辑草稿和相关备注，恢复“先选菜，再选择点餐时间”的流程。
- 主界面选菜后进入“已选菜单”，可调整数量、填写单个菜品的口味备注，并填写整餐备注。
- “已选菜单”左上角提供“返回”，返回主界面继续加菜时必须保留已选内容。
- “已选菜单”底部只保留一个主按钮“选择点餐时间”，不重复提供“继续加菜”“点餐时间”等按钮。
- “选择日期与餐次”页面左上角提供“返回”，右上角提供“×”关闭。
- 选择任何日期和餐次后，无论该餐是否已有菜单，都进入统一的“本餐菜单”确认页，不直接提交，也不跳转到重复填写备注的页面。
- “本餐菜单”页面左上角提供“返回”，右上角提供“×”，底部只保留一个主按钮“提交订单”。
- 提交后只完成点餐并轻提示采购清单已同步，不强制跳转采购清单；从点餐记录进入编辑时，提交后返回原日期记录。
- 空菜单不能提交，应提示用户至少添加一道菜。

### 同一餐次的多人协作

- 同一个日期和餐次可以由多位家庭成员先后点菜。
- 查看已有菜单时，要同时展示已有菜品和本次新增菜品，并明确显示每道菜由哪位家庭成员、在什么时间点餐。
- 所有成员都可以新增菜品、删除单个菜品、修改口味或删除整份未来菜单，不区分点餐人和掌勺人。
- 不设置“待确认”“已确认”或锁定菜品状态；菜单唯一有效状态是当前保存版本。
- 每次保存都应保留修改人和时间，重要修改应支持查看历史版本或撤销，避免多人协作误操作。
- 在日期和餐次之间来回切换时，不得把上一个餐次的已有菜单误带到另一个餐次；用户本次尚未提交的新选菜品应妥善保留。

### 菜单、采购与记录闭环

- 菜单一经保存便立即按日期和餐次汇总食材，不增加接单、确认或“生成准备计划”步骤。
- 采购清单是菜单的实时派生结果，不是独立维护的数据源；新增菜品追加食材，删除菜品移除尚未采购的食材，修改数量同步差额。
- 采购选择独立于菜单：新生成的食材默认全部勾选并列入采购，用户只需取消不需要购买的少数食材。菜单变化后保留仍存在食材的勾选状态，新增食材默认勾选，删除菜品时同步移除不再需要的食材。
- 不要求用户在做饭前点击“开始制作”。完成后可以直接在点餐记录中标记完成，成品照不是必填项。
- 完成后自动写入家庭饮食记录，并可生成带产品二维码的菜单图片，方便分享到微信等平台。
- 饮食记录应形成家庭营养趋势，并据此推荐后续菜谱，同时兼顾“很久没吃的菜”。

### 交互与视觉原则

- 优先保证简单、直观、低学习成本，减少重复页面、重复按钮和无实际价值的确认步骤。
- “今天、明天、后天”和日期选择器必须根据设备当前日期动态计算，不得在界面或逻辑中写死演示日期；自选日期保存的菜单键必须与点餐记录日历使用同一日期规则。
- 底部一级导航只在“点菜、菜谱／我家菜谱、点餐记录、我的”等一级页面显示；菜谱详情、菜谱编辑、营养分析及各类二级页面必须隐藏一级导航，避免覆盖返回键、保存按钮或底部主操作。
- 页面展示的菜谱数、点餐数、收藏数和采购项数必须由当前真实数据计算，不能使用与列表内容不一致的装饰性数字。
- 任何看起来可以点击的操作都必须产生与文案一致的状态变化或进入真实页面；尚未实现的能力应明确标注，不得仅用成功提示伪装为已完成。
- 可以借鉴美团等成熟点餐产品的选菜效率和信息层级，但所有流程必须适配家庭共同点菜，而不是模拟外卖下单。
- 按钮文案应直接表达下一步动作；“返回”和“×”分别承担回到上一步与直接关闭的含义，不应混用。
- 已有菜单与本次新增应清晰可辨；保存后合并为同一个当前菜单版本，不继续制造额外状态。
- “点菜”“菜谱”“点餐记录”“我的”等一级页面使用统一的原生 App 顶部栏：主标题在上、简短说明在下、右侧最多一个轻量操作；标题字号、留白、对齐和按钮规格保持一致。
- 一级页面顶部栏与详情页返回栏采用不同层级，不能把网页式大标题、重复说明或多个操作堆在页面顶部。

## 家庭成员角色界面核心需求

APP 不再区分“掌勺人”和“其他家庭成员”的功能界面或操作权限。所有人使用同一套家庭界面，可以点菜、编辑未来菜单、维护家庭菜谱和更新采购进度；不得要求用户切换角色或工作台。

### 统一家庭界面导航

- 家庭成员端底部导航为“点菜”“菜谱”“点餐记录”“我的”；“菜谱”是解决新家庭内容冷启动和持续扩充家庭菜谱的主要功能。
- “点菜”页面只负责浏览菜谱、选择菜品和完成点餐，不展示“点餐日历”和“掌勺人专属管理”。
- 原“家人点单”和“饮食记录”合并为“点餐记录”，避免多个入口重复展示家庭菜单。
- “点餐记录”同时承载历史记录、未来菜单和当天采购清单，不设置待确认菜单或掌勺人专属入口。
- 采购清单不独立占用底部导航，也不设置“今天的采购清单”等额外入口按钮；它由当天早餐、午餐、晚餐菜单实时派生，并直接作为晚餐后的第四张内容卡片展示。

### 点餐记录

- 用户可以通过日历查看任意一天，包括历史、今天和未来的点餐记录。
- 日历要直观区分历史饮食记录、未来已点餐和当前选中日期，并提供“今天”快捷入口。
- 每天按照早餐、午餐、晚餐展示；每道菜应显示点餐成员、点餐时间、口味备注和当前状态。
- 历史菜单只能查看，可以提供“再点一次”，但不可直接修改历史数据。
- 未来未点餐的餐次提供“去点菜”；未来已有菜单统一提供“编辑菜单”。
- 编辑未来菜单必须复用现有“点菜”页面和已选菜单能力，不重复开发另一套菜品编辑器。
- 从点餐记录进入编辑模式时，应自动带入日期、餐次和已有菜品；提交后返回原日期，菜单与下方采购卡片同步刷新。
- 采购卡片随当前日历日期变化；历史日期不展示采购卡片，今天和未来日期在三餐之后直接展示当日汇总。
- 每餐可查看本餐食材来源，整日采购清单按食材分类合并，并明确标注“餐次 · 菜品”来源。
- 采购卡片中的勾选框表示“需要购买”，未勾选自然表示“无需购买”，不再额外设置“家中已有”或“未标记”状态；所有食材默认勾选，并提供“取消全选／全部需买”批量操作。
- 采购卡片右下区域提供“查看三餐详情”和“分享采购清单”。详情按早餐、午餐、晚餐分别展示菜品、食材、用量与采购状态。
- 用户无需完成额外归类即可随时生成分享图；分享按钮显示当前需要购买的食材数量，分享图同时包含全天汇总和早、午、晚明细，清晰区分“需要购买”和“无需购买”，并支持保存到相册或调用微信、系统分享。
- “点菜”页不放“看记录”或采购清单快捷入口；底部导航已经提供稳定入口时，不再用页面按钮重复导航。

### 营养分析与菜谱推荐

- 营养分析不占用“点餐记录”的主要版面；家庭成员从“我的”页面点击“营养分析”后，进入独立的深度分析页面。
- “我的”只展示低视觉权重的营养摘要入口，详细图表、周期切换和建议在二级页面渐进呈现。
- 营养分析以家庭菜单估算为依据，避免展示无法保证准确的个人摄入量或过度精确的热量数据。
- 分析页面提供近 7 天、近 30 天和近 3 个月趋势，重点展示记录覆盖、食物类别搭配、食材多样性以及高油高盐出现频率。
- 页面采用“总体摘要—类别搭配—长期趋势—值得关注—推荐菜谱”的信息层级，先给结论，再允许用户查看详细依据。
- 使用温和、可行动的自然语言给出改善建议，不使用制造焦虑的评分、惩罚或强烈警告。
- 推荐菜谱需说明推荐理由，至少覆盖“补充近期不足”“减少重复”“好久没吃”和“当季推荐”。
- 推荐菜谱点击后直接复用点菜流程，可加入菜单并继续选菜。
- 推荐不作为独立一级功能，而是公共“菜谱”中的一种内容组织方式；点餐记录专注于查看和编辑过去、今天及未来的菜单。
- 菜谱库中的个性化内容必须解释推荐原因，并综合近期饮食、家庭成员喜好、收藏、距离上次食用时间、烹饪时间与当季食材，不能只按热门程度排序。

### 公共菜谱、我家菜谱与点菜

- “发现菜谱”和“我家菜谱”属于同一个“菜谱”一级模块，通过顶部视图切换进入；不再维护任何角色专属的菜谱编辑入口。
- 菜谱一级页顶部不再重复放“我家菜谱”按钮，只保留页面内“发现菜谱／我家菜谱”视图切换。
- 所有成员都可以查看和直接编辑我家菜谱；保存时生成新的家庭版本，保留修改人、修改时间和上一版本，支持查看历史与撤销。
- 所有成员都可以调整家庭版本的“开放点菜”“想学先存”和下架状态；高风险操作应二次确认，而不是依赖角色权限。
- “菜谱”是内容丰富的公共菜谱库，用于发现、搜索和学习；“我家菜谱”是家庭已收录并可持续改良的家庭资产；“点菜”只展示我家菜谱中已标记为“开放点菜”的菜。
- 公共菜谱页支持按菜名、食材、菜系、口味和做法搜索，并提供菜系、餐次、主要食材、场景、烹饪方式、时间、难度和设备等多维分类。
- 公共菜谱首页优先展示“全部、早餐、快手菜、家常菜、汤羹、素菜、烘焙、其它分类”等高频一级入口；“其它分类”通过弹层展示菜系、食材、做法、场景等低频分类。
- 用户进入菜谱页时默认选中“全部”，直接展示丰富菜谱流；用户可以在卡片视图和列表视图之间切换，分类筛选只改变下方菜谱结果，不产生重复的底部分类画板。
- 顶部分类导航与菜谱流之间可以设置自动轮播的美食内容横幅，用于新家庭快速开始、当季专题、早餐灵感等内容；轮播必须使用美食图片、提供分页指示和手动切换，并且不能替代主要分类导航。
- 菜谱页底部不再重复设置“按菜系浏览”；粤菜、湘菜、川菜、东北菜等菜系统一收纳到“其它分类”弹层中。
- 新家庭应提供“家庭入门菜谱”多选能力，让用户批量选择常做菜加入我家菜谱，不要求逐道创建。
- 任意成员将公共菜谱加入我家菜谱时，必须选择“我家会做，开放点菜”或“想学，先保存”；未标记为可点菜的菜不得出现在点菜页面。
- 从公共菜谱加入后生成独立的家庭版本，保留来源和原作者信息；家庭成员可以修改食材、用量、步骤、口味、心得和成品照，公共原版更新不得覆盖家庭版本。
- 已开放点菜的家庭版本仍可继续改良，但修改过程中应保持点菜状态明确，避免家庭成员看到不可制作或尚未验证的版本。

### 今日推荐

- “今日推荐”是点菜页中的轻量辅助卡片，固定放在搜索框下方、点菜菜谱上方，不新增一级导航，也不替代用户自行搜索和分类浏览。
- 卡片首屏展示推荐主题、推荐菜缩略信息和推荐理由，右下角提供“换一组”“调整推荐偏好”“选用推荐”三个快捷操作；“查看推荐”进入完整推荐详情，算法、来源和数量设置仍放在二级弹层。
- 默认采用“好久没吃”算法、只从我家菜谱推荐 3 道菜；可选“均衡搭配”“轻盈少负担”“香辣过瘾”“快手不费事”等偏好，名称使用生活化表达，不暴露技术算法概念。
- 推荐来源提供“只从我家菜谱”“我家＋发现菜谱”“只从发现菜谱”三种模式；当我家菜谱数量不足时，允许用户主动扩大推荐范围，而不是系统静默混入外部菜谱。
- 推荐数量默认 3 道，并允许选择 4、5、6 道；设置保存后立即重新推荐，卡片摘要同步显示当前主题、来源和实际推荐数量。
- 推荐详情需展示每道菜的推荐理由、简短介绍及来源。用户可以刷新整组推荐，也可以取消组内个别菜品，最终一键将选中的菜加入“已选菜单”。
- 选中来自发现菜谱的菜时，系统自动创建家庭版本、加入我家菜谱并开放点菜；该菜在点菜页只显示一条低权重信息“首次引入我家菜谱”，不重复展示来源、版本或刚刚加入等说明，保留一天后自动恢复为普通家庭菜谱状态。
- 外部菜谱自动引入后必须参与正常的菜单、食材和采购清单计算，后续可在我家菜谱中继续编辑和改良。

## 主功能与“锦上添花”功能的设计原则

设计任何功能前，必须先判断它属于主功能还是“锦上添花”功能，并采用不同的产品策略。

### 主功能

- 主功能是大多数用户完成核心目标时必须经过的能力，即选菜、选择点餐时间、编辑未来菜单，以及查看随菜单实时变化的采购清单。
- 主功能必须处于清晰、稳定、容易发现的位置，流程短、按钮少、文案直接，不要求用户学习复杂规则。
- 不得让低频功能打断主功能，不得为了展示功能数量而增加步骤、页面、弹窗或必填项。
- 每个页面应优先突出一个主要任务和一个主要操作，次要操作应降低视觉权重。

### “锦上添花”功能

- “锦上添花”功能是部分用户会喜欢、但不影响核心任务完成的低频或情感化能力，例如上传成品照、添加回忆文字、生成分享图片。
- 此类功能应通过当前场景和状态自然出现，不新增一级导航，不占据主页面首屏，不使用常驻悬浮按钮，也不在主流程中强制询问。
- 入口应保持可发现但低打扰，优先使用卡片底部的文字按钮、详情页中的次级操作或“更多”菜单。
- 用户忽略此类功能时，主流程必须仍然完整结束，系统也不能反复提醒或制造未完成感。
- 只有在用户主动使用后，才逐步展示裁剪、说明、分享样式等后续选项，遵循渐进式呈现。
- 微信等成熟产品将低频能力隐藏在具体场景或二级入口中的思路可以借鉴，但不能隐藏到用户完全无法发现。

## 成品照与家庭菜单分享

- 任意家庭成员将一餐标记为完成后，系统立即写入点餐记录；上传照片不是完成制作的前置条件。
- 家庭任意成员都可以为已完成的一餐补充成品照，不限定必须由掌勺人上传。
- “添加成品照”仅在已完成餐次的记录卡片或详情页中显示，使用低视觉权重的次级文字按钮，不影响查看菜单这一主要任务。
- 点击“添加成品照”后，通过轻量底部弹层选择“拍照”或“从相册选择”；照片说明为选填，并自动记录上传成员和时间。
- 同一餐允许家庭成员继续添加多张照片，形成家庭共享的小型相册；默认使用首张或家庭成员选定的照片作为封面。
- 照片上传成功后，餐次卡片自然展示缩略图，并出现次级操作“分享这一餐”；未上传照片时不突出分享能力。
- “分享这一餐”生成适合微信等平台的菜单图片，可包含日期、餐次、菜品、成品照、家庭名称和产品二维码。
- 分享前提供简洁预览，并允许关闭家庭名称、上传成员等可能涉及隐私的信息；默认不展示敏感家庭信息。
- 分享是用户主动触发的系统分享或保存图片操作，不在制作完成或上传照片后自动弹出。
- 除分享单餐外，允许用户主动选择起止日期，自动汇总该时间段内拥有成品照的餐次，制作阶段性的“家庭饮食回忆”。
- 多餐分享属于更低频的“锦上添花”能力，入口放在“我的”中的“制作回忆”，不出现在点餐记录、底部导航或点菜主流程中。
- “我的”中的“分享回忆”统计入口在功能界面尚未明确前不跳转，仅保留信息展示；“制作回忆”承担当前可用的区间照片整理能力。
- 系统自动汇总后，用户必须可以进一步勾选或移除餐次，并编辑分享标题和说明；不得把区间内所有照片不可修改地直接生成。
- 多餐分享图应以成品照为主体，清晰标注日期、餐次和菜品，并复用单餐分享的隐私、二维码、系统分享和保存图片规则。

## 统一使用场景

- APP 使用同一套“点菜、菜谱、点餐记录、我的”界面，不区分掌勺人、普通成员、我的餐桌或家庭餐桌。
- “我的”页面不展示“当前餐桌”及餐桌切换入口，避免重新引入已经合并的角色或空间概念。
- “我家菜谱”不作为独立主功能或底部导航，而是“菜谱”模块中的管理视图；用户从公共菜谱发现内容，在我家菜谱中编辑家庭版本和调整“开放点菜／想学先存”状态。
- 菜单提交后立即生效并生成采购清单，不发送待确认提醒，也不跳转角色工作台。
- 若食材无法买到，用户从同一日期记录进入对应餐次编辑；提交后返回原日期，采购卡片自动重新计算。
